import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "CoolTrip",
  description: "CoolTrip est l’assistant de voyage ultime qui transforme le chaos administratif en une simple formalité. Conçue pour les globe-trotteurs modernes, l’application centralise tout ce dont vous avez besoin pour franchir les frontières sans stress.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
